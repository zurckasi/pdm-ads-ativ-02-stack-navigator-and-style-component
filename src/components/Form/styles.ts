import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
        width: "90%",
        padding: 18,
        backgroundColor: "#FFFFFF",
        borderRadius: 5,
        marginTop: 1,
        fontSize: 20,
    },
    const: {
        marginTop: 30,
    },
    btnCategory: {
        borderRadius: 8,
        padding: 35,
        paddingTop: 10,
        paddingBottom: 10,
        borderWidth: 1,
        borderColor: "#c5c8cf",
    },
    picke:{
        backgroundColor: "#FFFFFF",
        marginTop: 15,
        padding: 5,
        borderRadius: 5,
    }
});
